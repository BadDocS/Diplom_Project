<Window x:Class="Course_Project.Pages.Foremen.Create_template"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Course_Project.Pages.Foremen"
        mc:Ignorable="d"
       SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" Background="#b8d0e0">
    <Grid Margin="10"  >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Height="30" Width="250" FontSize="16">Введите название шаблона:</TextBlock>
        <TextBox Name="tbTitle" Grid.Row="1" MinWidth="250" Height="30" Width="250"></TextBox>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Style="{StaticResource CancelBtn}" IsCancel="True">Отмена</Button>
            <Button Style="{StaticResource AcceptBtn}" IsDefault="True" Click="Accept_Click" Margin="10,0,0,0" Cursor="Hand">Подтвердить</Button>
            
        </StackPanel>
    </Grid>
</Window>
